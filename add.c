#include <stdio.h>

int main() {
    int num1, num2, mult;
    
    printf("Enter the first number: ");
    scanf("%d", &num1);
    
    printf("Enter the second number: ");
    scanf("%d", &num2);
    
    
    mult = num1 * num2;
    
    printf("The mult of the two numbers %d and %d is %d.", num1, num2, sum);
    
    return 0;
}
